import { Product } from "./product.model";

export class Cart {
  product: Product;
  quantity: number;
//   constructor(data) {
//     if (data) {
      
//       // tslint:disable-next-line: forin
//       for (const cart in data) {
//         console.log(cart)
//       //  this[fieldName] = data[fieldName] ? data[fieldName] : null;
//         this.totalQuantity += cart['quantity'];
//       }
//     }
//   }
}
